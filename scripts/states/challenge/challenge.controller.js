(function () {

    'use strict';

    angular
    .module('app.challengeState')
    .controller('challengeCtrl', challengeCtrl);

    challengeCtrl.$inject = [];

    function challengeCtrl() {
        var vm = angular.extend(this, {});
        return vm;
    }

})();
