(function () {

    'use strict';

    angular
    .module('app.queueState')
    .controller('queueCtrl', queueCtrl);

    queueCtrl.$inject = [];

    function queueCtrl() {

        var vm = angular.extend(this, {});

        return vm;
    }

})();
